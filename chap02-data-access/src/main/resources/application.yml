spring:
  webflux:
    hiddenmethod:
      filter:
        enabled: true

  devtools:
    restart:
      exclude: static/**,public/**

  thymeleaf:
    cache: false

logging:
  level:
    web: DEBUG

management:
  endpoint:
    health:
      show-details: always